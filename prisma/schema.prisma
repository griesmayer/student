datasource db {
    provider = "postgresql"
    url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client"
  runtime = "deno"
  output = "./generated"
}

model Student {
    id     Int    @id @default(autoincrement())
    name   String
    course String
}
