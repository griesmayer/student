datasource db {
    provider = "postgresql"
    url      = env("DATABASE_URL")
}

generator client {
    provider = "prisma-client"
    output   = "generatedclient"
    runtime  = "deno"
}

model Student {
    id     Int    @id @default(autoincrement())
    name   String
    course String
}
